{
  "compilerOptions": {
    // 启用项目引用功能，允许此配置文件被其他TypeScript项目引用
    "composite": true,

    // 跳过对声明文件的类型检查，提高编译速度
    "skipLibCheck": true,

    // 指定模块系统，使用最新的ES模块系统
    "module": "ESNext",

    // 模块解析策略，使用打包工具（如Vite）的解析方式
    "moduleResolution": "bundler",

    // 允许从没有默认导出的模块中默认导入
    "allowSyntheticDefaultImports": true
  },

  // 指定要包含在编译中的文件，这里只包含Vite配置文件
  "include": ["vite.config.ts"]
}
